import styled from "styled-components";

export default function Contacts(){
    return(
        <StyledContacts>
            <div>Contacts</div>
        </StyledContacts>
    )
}

const StyledContacts = styled.div`
    
`